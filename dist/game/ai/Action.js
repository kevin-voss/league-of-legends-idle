                                                        
                                                      

/**
 * A single candidate behaviour the utility brain can choose each tick.
 *
 * Every action scores itself (0–100) against the champion's current situation;
 * the brain runs the highest scorer. `execute` is given `dt` so it can drive the
 * champion's movement and combat for the frame it is selected.
 */
                         
                        
                                                                    
                                                                       
 
